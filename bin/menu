#!/bin/bash
# shellcheck source=/home/spin/.cache/wal/colors.sh
. ~/.cache/wal/colors.sh
dmenu_run -l 10 -p "run:" -fn Iosevka:pixelsize=20 \
  -nb "$background" -nf "$foreground" \
  -sb "$color2" -sf "$background"
